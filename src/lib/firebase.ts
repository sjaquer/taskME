
/**
 * ARCHIVO DEPRECADO
 * 
 * Este archivo ha sido desactivado para evitar fugas de memoria y conexiones duplicadas a Firebase.
 * Toda la inicialización de Firebase se gestiona centralizadamente en `src/firebase/index.ts`.
 */
export const auth = null;
export const db = null;
